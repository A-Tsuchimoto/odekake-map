{
  "name": "odekake-map",
  "private": true,
  "version": "1.0.0",
  "description": "おひさまマップ - 家族お出かけマップ（Cloudflare Workers）",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:spots": "node scripts/build-spots.mjs",
    "check": "node scripts/build-spots.mjs --check && node scripts/check-config.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
